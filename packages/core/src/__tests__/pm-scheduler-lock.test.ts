/**
 * BEC-200: pg_advisory_lock contention tests — consolidated in pm-scheduler-advisory-lock.test.ts
 *
 * All test coverage for the scheduler's pg_advisory_lock contention path is in
 * pm-scheduler-advisory-lock.test.ts. This file is intentionally empty to avoid
 * duplicate test output and potential mock conflicts.
 */
