/**
 * BEC-185 Reproduction Test — SUPERSEDED
 *
 * This file has been superseded by the fix in:
 *   packages/core/src/sync/gh-linear-sync.ts
 * and the updated tests in:
 *   packages/core/src/__tests__/gh-linear-sync.test.ts
 *
 * The original reproduction tests for the AND-semantics bug are no longer
 * applicable now that the fix uses per-label calls for OR semantics.
 *
 * This file is intentionally empty and will be removed in the same commit
 * as the fix (git rm).
 */

// No tests — file scheduled for deletion alongside the BEC-185 fix commit.
export {};
